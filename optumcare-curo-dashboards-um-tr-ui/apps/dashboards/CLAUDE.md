# Claude Repo Entry Point

Load and follow `./AGENTS.md` as the canonical repository instruction file.

Do not duplicate repo standards here. Keep naming conventions, data retrieval patterns, and UI experience rules in `AGENTS.md` so all sessions and all developers inherit one shared source of truth.

If a new repo-wide pattern is introduced, update `AGENTS.md` first and keep this file as the compatibility shim.
